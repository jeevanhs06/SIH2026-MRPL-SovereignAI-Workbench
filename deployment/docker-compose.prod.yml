services:
  backend:
    build:
      context: ../backend
    env_file:
      - ../.env.example
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - sovereignai_data:/workspace/data
      - ../knowledge_base:/workspace/knowledge_base:ro

  frontend:
    build:
      context: ../frontend
    restart: unless-stopped
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  sovereignai_data:
