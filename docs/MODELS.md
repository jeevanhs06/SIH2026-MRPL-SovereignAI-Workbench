# Models (Starter)

Current scaffold uses `mock-local-provider` in `backend/app/services/model_gateway.py`.

## Next steps

- Integrate local model runtime (e.g., Ollama/llama.cpp).
- Add model profiles by task type (text, OCR-assist, vision, embeddings).
- Add runtime availability checks and graceful fallback policy.
